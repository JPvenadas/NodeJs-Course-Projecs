const {
         Readfile, 
         WriteFile,
         DeleteFile,
         CreateFolder,
         RemoveFolder,
         ReadfileStream,
         WritefileStream
} = require('./function')


// put the commands here || (below)

WritefileStream("./greet.txt", " this is the second line")


