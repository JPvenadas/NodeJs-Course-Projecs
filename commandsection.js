const {
         Readfile, 
         WriteFile,
         DeleteFile,
         CreateFolder,
         RemoveFolder
} = require('./function')


// put the commands here || (below)

RemoveFolder('./folder')

